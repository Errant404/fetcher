[project]
name = "fetcher"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "scrapling[all]>=0.4.7",
]

[tool.fastapi]
entrypoint = "src.main:app"